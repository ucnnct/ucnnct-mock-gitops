apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  labels:
    app.kubernetes.io/name: orchestrator
    app.kubernetes.io/part-of: ucnnct-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: orchestrator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: orchestrator
        app.kubernetes.io/part-of: ucnnct-mock
    spec:
      serviceAccountName: orchestrator
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: orchestrator
          image: ghcr.io/ucnnct/mock-orchestrator:0.1.0
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "7300"
            - name: WORKER_SERVICE_ORIGIN
              value: http://worker-service:7400
            - name: MOCK_USER_SERVICE_ORIGIN
              value: http://mock-user-service:7500
            - name: K8S_NAMESPACE
              value: ucnnct-mock
            - name: WORKER_DEPLOYMENT_NAME
              value: worker-service
            - name: WORKER_HPA_NAME
              value: worker-service
            - name: WORKER_LABEL_SELECTOR
              value: app.kubernetes.io/name=worker-service
            - name: WORKER_SHARD_SIZE
              value: "250"
            - name: IDENTITY_REUSE_FACTOR
              value: "25"
            - name: WORKER_MIN_REPLICAS
              value: "2"
            - name: WORKER_MAX_REPLICAS
              value: "40"
            - name: MAX_VIRTUAL_USERS
              value: "10000"
          ports:
            - name: http
              containerPort: 7300
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 512Mi
