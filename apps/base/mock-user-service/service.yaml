apiVersion: v1
kind: Service
metadata:
  name: mock-user-service
  labels:
    app.kubernetes.io/name: mock-user-service
    app.kubernetes.io/part-of: ucnnct-mock
spec:
  selector:
    app.kubernetes.io/name: mock-user-service
  ports:
    - name: http
      port: 7500
      targetPort: http
