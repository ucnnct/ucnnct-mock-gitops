apiVersion: v1
kind: Service
metadata:
  name: worker-service
  labels:
    app.kubernetes.io/name: worker-service
    app.kubernetes.io/part-of: ucnnct-mock
spec:
  selector:
    app.kubernetes.io/name: worker-service
  ports:
    - name: http
      port: 7400
      targetPort: http
