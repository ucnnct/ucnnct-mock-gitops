apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: ucnnct-mock-staging-image-updater-orchestrator
  namespace: argocd
spec:
  namespace: argocd
  commonUpdateSettings:
    updateStrategy: semver
    allowTags: 'regexp:^v?[0-9]+\.[0-9]+\.[0-9]+$'
    pullSecret: pullsecret:argocd/ghcr-credentials
  writeBackConfig:
    method: git
    gitConfig:
      branch: main
      repository: https://github.com/ucnnct/ucnnct-mock-gitops.git
      writeBackTarget: kustomization:./orchestrator
  applicationRefs:
    - namePattern: ucnnct-mock-staging
      images:
        - alias: orchestrator
          imageName: ghcr.io/ucnnct/mock-orchestrator
          commonUpdateSettings:
            forceUpdate: true
          manifestTargets:
            kustomize:
              name: ghcr.io/ucnnct/mock-orchestrator
